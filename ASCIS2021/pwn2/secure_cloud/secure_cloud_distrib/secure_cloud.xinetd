service secure_cloud
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = root
    bind        = 0.0.0.0
    server      = /home/secure_cloud/secure_cloud
    type        = UNLISTED
    port        = 9999
}
